services:
  requiem-discord:
    build: .
    container_name: requiem-discord
    network_mode: host
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config.yaml:/app/config.yaml:ro
